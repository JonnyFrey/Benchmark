package benchmark.sort;

/**
 * A class that can sort an array of integers.
 */
@FunctionalInterface
public interface Sort {

    /**
     * Sorts the array of integers in ascending order.
     * @param sort the array of integers to sort
     */
    void sort(int[] sort);

}
